## Decisions

- Removed the unused `VoiceItem` implementation from `app/screens/ai-voice.tsx` instead of migrating it, since the screen uses `VoiceSelectCard` and the old component was commented out.
